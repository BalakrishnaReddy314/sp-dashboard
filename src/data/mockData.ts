import type { DocumentRecord, FileType, LifecycleStatus, MetadataChoice, Sensitivity } from '../types/documents';

export const domains = [['FIN', 'Finance & controlling'], ['HR', 'People & culture'], ['ENG', 'Engineering'], ['LEG', 'Legal & compliance'], ['OPS', 'Operations'], ['SEC', 'Security'], ['MKT', 'Marketing'], ['SAL', 'Sales']] as const;
const names = ['Policy review', 'Quarterly assessment', 'Audit protocol', 'Compliance guide', 'Operating framework', 'Technical specification', 'Executive summary', 'SLA standard', 'Architecture blueprint', 'Risk evaluation', 'Procurement schedule', 'Strategic roadmap', 'Vendor agreement', 'Incident playbook', 'Data governance schema', 'Staffing model', 'Budget forecast', 'Recovery plan', 'Quality assurance check', 'Release schedule'];
export const metadataChoices: MetadataChoice[] = Array.from({ length: 200 }, (_, index) => { const [code, domain] = domains[index % domains.length]; return { id: `${code}-${String(Math.floor(index / domains.length) + 1).padStart(3, '0')}`, name: names[index % names.length], domain, count: 2 + ((index * 7) % 7) }; });
export const fileTypes: FileType[] = ['DOCX', 'PDF', 'XLSX', 'PPTX', 'ONE'];
export const lifecycleStatuses: LifecycleStatus[] = ['Approved', 'In review', 'Attention', 'Draft', 'Archived'];
export const sensitivities: Sensitivity[] = ['Public', 'General', 'Confidential', 'Restricted'];
const authors = ['David Vance', 'Sarah Jenkins', 'Amara Okafor', 'Alex Rivera', 'Karen Mitchell', 'Julian Rossi'];
const docNames = ['Annual strategy', 'Gap assessment', 'Standard operating procedure', 'Consolidated balance sheet', 'Implementation manual', 'Integration guide', 'Security baseline', 'Vendor onboarding plan', 'Information handling standard', 'Market analysis'];
export const documents: DocumentRecord[] = Array.from({ length: 450 }, (_, index) => { const choice = metadataChoices[(index * 13) % metadataChoices.length]; return { id: `DOC-${String(index + 1).padStart(4, '0')}`, title: `${choice.id.split('-')[0]} ${docNames[index % docNames.length]} - ${choice.name}`, choice, type: fileTypes[index % fileTypes.length], author: authors[index % authors.length], classification: index % 2 === 0 ? 'Type 1' : 'Type 2', status: lifecycleStatuses[index % lifecycleStatuses.length], sensitivity: sensitivities[index % sensitivities.length], date: `2026-12-${String(28 - (index % 18)).padStart(2, '0')}`, size: `${(0.8 + (index % 17) * 0.9).toFixed(1)} MB` }; });
